; logging file for development

{
 ; min-level oder matters: it is greedy!
 :min-level [[#{"mqtt2pg.message-handlers.shelly"
                "mqtt2pg.insert"
                } :debug]
             [#{"mqtt2pg.*"} :info]
             [#{"*"} :warn]]
 ; set log-level to nil, otherwise it will take precedence over min-level
 :log-level nil
 }

#!/bin/bash
set -eux

PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd .. > /dev/null 2>&1 && pwd -P)"

cd $PROJECT_DIR

touch mqtt2pg
chmod a+x mqtt2pg
npx shadow-cljs watch service

# vim: syntax=sh
